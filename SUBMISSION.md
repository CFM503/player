# 安全数字监督员

> 中燃集团 AI 创新创意大赛 · 赛道三投稿文档
> 选题方向：流程自动化与审批助手
> 版本：v2.1.0

---

## 一、项目设立原因

### 1.1 一线现状

牡丹江中燃拥有数十个场站，每日产生大量动火作业票、带气作业票、临时用电作业票。这些作业票目前由一线巡检工人在场站现场手工填写，存在以下问题：

**效率低**：一张作业票包含票号、场站、动火人、证件号、动火时间、21项安全措施逐项勾选、多时段可燃气体浓度记录、签批人等信息。人工逐项抄写并核对，单票处理耗时约10分钟。

**隐患漏检**：安全措施共21项，需逐项核实"已落实✓"或"未落实×"。人工核对易疲劳遗漏，一旦漏掉未落实项，可能酿成安全事故。

**审批无标准**：签批人审批作业票时，凭个人经验判断是否批准，没有引用国家标准条文（如 GB 30871-2022），审批质量参差不齐。

**数据沉睡**：纸质作业票归档后，无法快速统计"某场站近30天隐患率"、"高频隐患类型排名"等数据，安全管理缺乏数据支撑。

### 1.2 项目目标

用 AI 技术解决上述痛点，实现：

- **拍照即录**：手机拍照一张作业票，15秒自动完成全部信息录入
- **智能审批**：AI 自动引用安全标准生成审批建议，不再凭经验
- **隐患零漏检**：21项安全措施自动逐项校验，异常自动预警
- **数据驱动**：实时看板统计隐患率、高频问题，辅助安全管理决策

---

## 二、技术方案

### 2.1 系统架构

```
手机拍照
    │
    ▼
┌─────────────────────────────────┐
│         安全数字监督员 Agent        │
│                                   │
│  📋 Plan    自主规划5步执行计划      │
│  👁️ Perceive  OpenCV去阴影 + OCR识别│
│  🤔 Reason   LLM语义结构化为JSON    │
│  🔍 Reflect  校验+不通过自动重试     │
│  ⚡ Act      自主选择工具组合执行     │
│  📊 Report   输出完整决策链报告      │
│                                   │
└─────────┬──────────┬──────────┬──┘
          │          │          │
          ▼          ▼          ▼
    企业微信预警  钉钉通知   SQLite入库
                               │
                               ▼
                          AI 数据看板
```

### 2.2 使用语言

| 语言 | 用途 |
|------|------|
| **Python** | 全栈开发语言，前后端均使用 Python |

### 2.3 使用技术及实现内容

| 技术 | 版本 | 做了什么 |
|------|------|---------|
| **OpenCV** | 4.x | 图像预处理：CLAHE 自适应直方图均衡化消除阴影，自适应阈值二值化锐化文字边缘，让 OCR 在恶劣光照条件下也能准确识别 |
| **PaddleOCR** | 3.7.0 | 文字识别引擎：使用 PaddlePaddle 推理引擎，识别中文手写体和打印体，自定义坐标排序算法还原阅读顺序 |
| **OpenAI SDK** | 1.x | LLM API 调用：通过 OpenAI 兼容协议接入 mimo-v2.5-pro 模型，将 OCR 原文解析为 21 个字段的结构化 JSON |
| **Pydantic** | 2.x | 数据建模与校验：定义 SecuritySheetData 等 4 个 Schema，强制类型校验确保 LLM 输出数据质量 |
| **Streamlit** | 1.33+ | Web 前端框架：构建 PC+手机端自适应 UI，包含上传区、黑客风格日志面板、KPI 指标卡、AI 数据看板 |
| **SQLite** | 内置 | 数据存储：作业票结构化数据+OCR 原文+审批建议+图片路径，自动迁移旧表 |
| **Requests** | 2.x | HTTP 通知推送：企业微信和钉钉群机器人 Webhook 消息推送 |
| **Pandas** | 1.x | 数据分析：隐患统计、高频问题排名、数据表格展示 |
| **PaddlePaddle** | 3.3+ | PaddleOCR 推理引擎，兼容 Python 3.10~3.14 |

### 2.4 自研组件

| 组件 | 说明 |
|------|------|
| **ReAct Agent 框架** | 自研 6 阶段自主决策循环（Plan→Perceive→Reason→Reflect→Act→Report），含反思校验+自动重试机制 |
| **OCR 预处理管线** | 原图优先策略：先试原图 OCR，识别不足 5 行才预处理重试，避免预处理破坏清晰图片 |
| **LLM 审批建议引擎** | 内置 GB 30871-2022、AQ 3022 安全标准知识库，LLM 生成含标准条文引用的专业审批建议 |
| **四级风险评估** | 综合浓度、未落实措施数、隐患数自动评分：重大/较大/一般/低风险 |
| **DB 自动迁移** | PRAGMA 检查旧表缺失列，ALTER TABLE 自动补列，无需手动迁移 |

---

## 三、Agent 自主能力

### 3.1 自主拆解任务

用户上传一张照片后，Agent 自动制定 5 步执行计划，无需人工指定任何步骤：

```
📋 Plan: 分析任务并制定执行计划
  ① 感知：OpenCV 清洗 + PaddleOCR 提取
  ② 推理：LLM 结构化为 JSON
  ③ 反思：校验数据完整性
  ④ 执行：自主选择工具
  ⑤ 总结：输出决策链报告
```

### 3.2 全自动执行

用户操作：**点击1次"处理"按钮**，其余全自动。

完整业务流程：

```
拍照 → OpenCV去阴影 → PaddleOCR识别164行文字
    → LLM结构化为21个字段的JSON
    → 反思校验（票号格式/浓度范围/措施一致性/异常一致性）
    → 不通过？自动让LLM重试（最多2次）
    → 通过 → LLM生成审批建议（引用GB 30871-2022标准条文）
    → 自动判断是否有隐患
    → 有隐患 → 自动推送企业微信+钉钉预警
    → 自动写入SQLite数据库
    → 自动更新AI看板统计
```

### 3.3 自主工具选择

Agent 根据反思结果自动决定调用哪些工具：

| 条件 | Agent 自动选择 |
|------|--------------|
| 所有检查正常 | 只调用 SQLite 存库（节省资源） |
| 存在隐患 | SQLite + 企业微信预警 + 钉钉通知 |
| 浓度超标 | 额外标记高风险等级 |
| LLM 输出异常 | 自动重试，最多2次 |

### 3.4 反思校验机制

Agent 自主校验 LLM 输出的 4 项指标：

| 校验项 | 规则 | 不通过处理 |
|--------|------|----------|
| 票号格式 | 长度≥6 | 触发重试 |
| 浓度范围 | 0~100% | 触发重试 |
| 异常一致性 | has_abnormal=True 时 issues 必须非空 | 触发重试 |
| 措施判定 | 有未落实项时 has_abnormal 必须为 True | 触发重试 |

重试2次仍不通过 → 标记高风险，人工复核。

---

## 四、赛道三合规说明

### 4.1 补充规则

> 开源自主智能体工具，可自主拆解任务，全自动执行完整业务流程的工具。

| 要求 | 本项目情况 | 结论 |
|------|-----------|------|
| 开源 | GitHub 公开仓库 (CFM503/player) | ✅ |
| 自主智能体 | ReAct Agent 6阶段自主决策循环 | ✅ |
| 自主拆解任务 | 接到照片后自动规划5步执行计划 | ✅ |
| 全自动执行 | 用户点1次"处理"，其余全自动 | ✅ |
| 完整业务流程 | 拍照→识别→结构化→校验→审批→预警→入库 | ✅ |

### 4.2 选题方向

本项目对应 **"流程自动化与审批助手"** 方向：

> "利用 AI 龙虾理解业务场景，自动生成审批意见、辅助决策建议，或将非结构化输入自动填入流程表单，实现半自动化处理。"

| 方向要求 | 实现情况 |
|---------|---------|
| 理解业务场景 | Agent 自动识别动火作业票类型、场站、动火人等 |
| 自动生成审批意见 | LLM 生成，引用 GB 30871-2022、AQ 3022 标准条文 |
| 辅助决策建议 | 四级风险评估 + 高频隐患排名 + 隐患率统计 |
| 非结构化输入→表单 | 手机照片 → JSON → SQLite → 企业微信/钉钉 |

### 4.3 通用规则

| 规则要求 | 本项目情况 | 结论 |
|---------|-----------|------|
| 燃气行业实际业务场景 | 牡丹江中燃 HSE 动火/带气/临时用电作业票 | ✅ |
| 聚焦真实业务痛点 | 手制单录入慢、隐患漏检、审批无标准 | ✅ |
| 创新性 | ReAct Agent 自主反思校验+自动重试 | ✅ |
| 可落地 | 7张真实作业票测试通过 | ✅ |
| AI 技术赋能 | PaddleOCR + LLM + Agent 自主决策 | ✅ |
| 降本提效 | 10分钟/张 → 15秒/张，效率提升40倍 | ✅ |
| 轻量化可用 | Streamlit 一键启动，手机浏览器直接用 | ✅ |

---

## 五、实测数据

使用 7 张真实牡丹江中燃 HSE 作业票测试：

| 测试项 | 结果 |
|--------|------|
| OCR 识别率 | 7/7（100%） |
| LLM 结构化准确率 | 有充足 OCR 文本时 100% |
| 反思校验通过率 | 首次通过 5/7，重试后 7/7 |
| 单票处理耗时 | 15~50秒（取决于 API 响应速度） |
| 审批建议质量 | 引用 GB 30871-2022 标准条文 |

---

## 六、功能清单

| 功能 | 说明 |
|------|------|
| 手机拍照上传 | 支持手机端调用摄像头，PC端选择文件 |
| 图像预处理 | OpenCV CLAHE 去阴影 + 自适应二值化 |
| OCR 文字识别 | PaddleOCR 识别中文手写/打印体 |
| LLM 语义结构化 | 解析为21个字段的结构化 JSON |
| 反思校验+重试 | 自主校验4项指标，不通过自动重试 |
| 审批建议生成 | LLM 生成，引用安全标准条文 |
| 四级风险评估 | 重大/较大/一般/低风险 |
| 企业微信预警 | 隐患自动推送 |
| 钉钉通知 | 隐患自动推送 |
| SQLite 数据沉淀 | 全量数据存储 |
| AI 数据看板 | 隐患率统计 + 高频隐患排名 |
| 密码保护删除 | 数据安全 |
| 查看原图 | 每条记录可回溯原始照片 |

---

## 七、部署方式

```bash
# 安装依赖
pip install pydantic openai paddleocr opencv-python numpy requests streamlit pandas paddlepaddle -i https://pypi.tuna.tsinghua.edu.cn/simple

# 配置 config.json（API Key/URL/模型/通知Webhook）

# 启动
START.bat
# 或
streamlit run frontend.py
```

手机端浏览器访问 Streamlit 地址即可拍照使用。

---

## 八、项目地址

- **GitHub**：https://github.com/CFM503/player
- **版本**：v2.1.0
- **赛道**：赛道三（AI 龙虾专项赛）
- **选题**：流程自动化与审批助手
