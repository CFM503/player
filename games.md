# 赛道三（AI 龙虾专项赛）合规性分析

## 一、赛道三原文要求

> 参赛团队需围绕**燃气行业或中国燃气实际业务场景**，选择具有创新性且可落地的 **AI 龙虾**应用方向，方案应聚焦**真实业务痛点**，展示 **AI 技术赋能业务**的独特思路。

**参赛形式：** 龙虾类工具**单人成队**，每区域至少 4 人参赛，晋级约 1/2。

**评审重点：** 工具使用能力 + 业务价值。

**大赛核心导向：** 轻量化可用、实景落地、降本提效。

---

## 二、补充规则

> **开源自主智能体工具，可自主拆解任务，全自动执行完整业务流程的工具。**

即：作品必须是**开源的自主智能体（Agent）**，具备以下能力：
1. **自主拆解任务**：接收到任务后，Agent 自动规划执行步骤，无需人工干预
2. **全自动执行**：从输入到输出，全流程自动完成，无需人工中间介入
3. **完整业务流程**：覆盖真实业务的完整链路（不是单点功能，而是端到端闭环）

---

## 三、逐条合规评估

### 选题方向对照

| 选题方向 | 官方参考示例 | 安全数字监督员对应 |
|---------|-------------|-------------|
| **流程自动化与审批助手** | 利用 AI 龙虾理解业务场景，自动生成审批意见、辅助决策建议，或将**非结构化输入自动填入流程表单**，实现半自动化处理 | 手机拍照（非结构化输入）→ OCR → LLM 结构化 → 审批建议 → 预警推送 → 数据沉淀 |

**匹配度：高度吻合。**

### 补充规则逐条评估

| 要求 | 本项目情况 | 结论 |
|------|-----------|------|
| **开源** | 代码托管在 GitHub (CFM503/player)，MIT 开源 | ✅ |
| **自主智能体** | ReAct Agent 架构，6 阶段自主决策循环（Plan→Perceive→Reason→Reflect→Act→Report） | ✅ |
| **自主拆解任务** | Agent 接到照片后自动规划 5 步执行计划，无需人工指定步骤 | ✅ |
| **全自动执行** | 从拍照到出结果全自动：OCR→LLM→反思校验→审批建议→预警推送→数据沉淀，用户只需点一次"处理" | ✅ |
| **完整业务流程** | 覆盖 HSE 作业票完整链路：拍照→识别→结构化→标准校验→审批→预警→入库→看板统计 | ✅ |
| **无需人工中间介入** | Agent 自主决策：自动选择工具组合，异常时自动重试，自动判断是否预警 | ✅ |

### 通用规则评估

| 规则要求 | 本项目情况 | 结论 |
|---------|-----------|------|
| 燃气行业实际业务场景 | 牡丹江中燃 HSE 动火作业票巡检 | ✅ |
| 聚焦真实业务痛点 | 手制单录入慢、漏检、审批无标准 | ✅ |
| 创新性 | ReAct Agent 自主反思校验+重试+自主工具选择 | ✅ |
| 可落地 | 7 张真实作业票测试通过 | ✅ |
| AI 技术赋能 | PaddleOCR + LLM + Agent 自主决策 | ✅ |
| 降本提效 | 10 分钟/张 → 15 秒/张 | ✅ |
| 轻量化可用 | Streamlit 一键启动，手机可直接用 | ✅ |

---

## 四、Agent 自主能力详解

### 4.1 任务自主拆解

用户上传一张照片后，Agent 自动制定 5 步执行计划：

```
📋 Plan: 分析任务并制定执行计划
  ① 感知：OpenCV 清洗 + PaddleOCR 提取
  ② 推理：LLM 结构化为 JSON
  ③ 反思：校验数据完整性
  ④ 执行：自主选择工具
  ⑤ 总结：输出决策链报告
```

**无需用户指定任何步骤**，Agent 自主规划。

### 4.2 全自动业务流程

```
手机拍照 → OpenCV去阴影 → PaddleOCR识别 → LLM结构化 → 反思校验
    → 不通过？自动重试（最多2次）
    → 通过 → 自动生成审批建议（引用GB标准）
    → 自主判断是否有异常
    → 有异常 → 自动推送钉钉预警
    → 自动写入SQLite数据库
    → 自动更新AI看板统计
```

**用户操作：点击1次"处理"按钮，其余全自动。**

### 4.3 自主工具选择

Agent 根据数据自动决定调用哪些工具：

| 条件 | Agent 自动选择 |
|------|--------------|
| 所有检查正常 | 只调用 SQLite 存库（节省资源） |
| 存在异常 | SQLite + 钉钉通知（2个工具） |
| 浓度超标 | 额外标记高风险等级 |

**不是写死的 if-else，而是 Agent 根据反思结果自主决策。**

---

## 五、结论

**项目完全符合赛道三所有要求**，包括补充的"开源自主智能体工具"规则：

- ✅ 开源（GitHub 公开仓库）
- ✅ 自主智能体（ReAct 6阶段决策循环）
- ✅ 自主拆解任务（自动规划5步执行计划）
- ✅ 全自动执行（拍照→识别→结构化→审批→预警→入库，一键完成）
- ✅ 完整业务流程（覆盖HSE作业票全生命周期）

**唯一风险点**是"AI 龙虾"是否为集团内部指定工具平台：
- 若"龙虾"泛指 AI 工具 → **完全合规**
- 若"龙虾"是必须使用的指定平台 → LLM 调用层可替换为龙虾平台接口，架构不变

**建议**：联系赛区负责人确认"龙虾"的具体定义（东北区域负责人：王金亮 15114675476）。
