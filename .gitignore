# 配置文件（含API密钥）
config.json

# 模型文件（太大，不提交）— 仅根目录 models/，不含 ocr 训练工作区
/models/

# Python
__pycache__/
*.pyc
*.pyo

# 运行时生成
*.db
workspace/
upload/
uploads/
archives/

# 文字 OCR 训练工作区（ocr9）— 允许提交裁剪样本与标注，便于多机协同
# 忽略体积大的原图、临时预览与 run 日志
ocr_train_workspace/raw/
ocr_train_workspace/runs/
ocr_train_workspace/_tmp_preview.png

# 勾选格训练工作区（ocr10）— 允许提交样本与导出，便于多机协同训练
# 忽略体积大的原图上传与临时 run 日志
ocr_mark_workspace/raw/
ocr_mark_workspace/runs/

# 临时文件
*.tmp
*.log
.dingtalk_cache.json
# 注意：不要用 _*.json 通配，以免误伤业务配置；仅忽略明确临时文件
*_final*.json
*去表格化.png
