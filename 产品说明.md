# 数字化安全监督员

> 面向燃气 HSE 场景的作业票合规监督与审批助手  
> 当前聚焦：《牡丹江中燃带气作业票》

---

## 功能

监督《带气作业票》填写合规性，对必填项、安全措施勾选与签批完整性进行自动校验：

- **25 项安全措施 × 每项 5 列确认格**（作业人 / 施工方现场负责人 / 监理 / 监护人 / 带气现场负责人）
- 每格合法填写：**落实 √ / 未落实 × / 不适用 \\**；空白视为漏项
- **表头作业等级**：一级 / 二级（**一级危险程度最高**）
- **日期与签名**：作业时间、完工时间、发起人签字及签批五列等须填写齐全

**审批分流：**

| 情况 | 结果 |
|------|------|
| 二级 + 无隐患 + 天气正常 | 自动审批通过 |
| 一级（危险最高）+ 无隐患 | **人工介入**：经 **MCP 推送钉钉 AI 表格**，主管在钉钉侧处理 |
| 有漏项 / 未落实 / 作业等级未识别 / 关键识别失败 | **禁止放行**：经 **MCP 推送钉钉 AI 表格**，主管在钉钉侧处理 |

> **人工介入定义**：不是系统内另设人工台，而是通过 **钉钉 MCP** 将审批意见与核心要素写入 **钉钉 AI 表格**，再由表格自动化发送钉钉消息给相关人员。

处理结果：

- 自动写入 **本地 SQLite 数据库**
- 经 **MCP 推送钉钉 AI 表格**（自动通过 / 待审批 / 已驳回均会写入，便于留痕与消息触达）
- 本地归档原图、对齐图、OCR、审批建议与决策链，便于审计追溯

---

## 优势

1. **WebUI 接入**：只要会用浏览器的手机或电脑，即可上传手填表格照片，无需安装专用客户端。
2. **可私有化、数据不出企**：支持本地 PaddleOCR 与本地/企业大模型，一线人员无需脱敏即可使用。
3. **端到端闭环**：拍照 → 识别 → 校验 → 审批建议 → 入库 → 钉钉通知，一点「处理」即可跑通全流程。
4. **校验可审计、禁止静默兜底**：规则明确（25×5、签批、作业等级）；识别不到不编造，问题暴露后修复链路，符合 HSE 可信要求。

---

## 技术实现

使用 Python 编写 **ReAct 智能体**，业务流程五个阶段：

### 一、感知

- OpenCV 模板对齐、去表格线等预处理
- **本地 PaddleOCR** 提取文字（预留视觉大模型接口，可接 Ollama 等）
- 带气票 **25×5 勾选格**由 OpenCV 符号识别（对号 / 叉号 / 斜杠 / 空白）写入结构化结果

### 二、推理

- **LLM** 将 OCR 文本整理为结构化字段（票号、作业单位、作业内容、签批姓名等）
- 规则引擎重组 25 项安全措施及落实状态；网格未识别到的格子记为漏项，**禁止默认「已落实」**

### 三、反思

- **以规则校验数据完整性为主**（非「LLM 目测字迹」）：
  - 25 项 × 5 列均已填写（√ / × / \\），无空白漏项
  - 日期与签名齐全
  - 表头作业等级已识别为一级或二级
- 校验不通过：有限次触发 LLM **重解析纠错**
- 仍不通过：标记异常，进入 **MCP → 钉钉 AI 表格** 人工介入路径，禁止静默用默认值放过

### 四、执行

- 按作业等级、隐患与天气做 **L3 条件路由**（自动通过 / 钉钉人工介入 / 禁止作业）
- 生成审批建议（引用如 CJJ 51-2016 等标准表述）
- 写入本地 SQLite
- 经 **MCP 推送钉钉 AI 表格**（人工介入与禁止放行的主通道），并由表格自动化发消息
- 审批建议与核心要素归档到本地 `archives/`

### 五、总结

- 输出完整 **决策链日志**（规划 → 感知 → 推理 → 反思 → 执行各步记录）
- 支撑复盘、审计与问题定位

---

## 合规校验要点（带气作业票）

| 校验项 | 要求 |
|--------|------|
| 安全措施 | 25 项齐全；每项 5 列均为 √ / × / \\ 之一 |
| 作业等级 | 表头填写一级或二级（一级危险最高） |
| 日期 | 签署日期、作业时间、完工时间等已填写 |
| 签名 | 发起人签字 + 签批五列已填写 |
| 未落实（×） | 记为业务隐患，MCP 推送钉钉 AI 表格（人工介入/禁止放行） |
| 识别失败 | 不编造字段；报错或记隐患，禁止兜底通过 |

---

## 部署与使用（概要）

1. 浏览器打开 Web 界面，配置本地/在线 LLM、可选钉钉 MCP 地址  
2. 上传带气作业票照片（或手机拍摄）  
3. 点击处理：智能体自动跑完五阶段  
4. 查看审批建议、作业等级、决策链日志；数据已入库并可推送钉钉  

---

## 说明

- 本产品说明对应当前 **带气作业票** 主流程；动火等其他票型不与带气逻辑交叉。  
- 反思阶段以 **规则完整性校验** 为核心，LLM 用于结构化提取与失败重试，不作为「字迹是否好看」的主观判定。  
- 版本与实现细节以代码及 `CHANGELOG.md` 为准。
